# TeraSim: saved assets, 2026-09-23

`main` is the maintained version used by TeraSim-Agent. `dev_old` preserves the previous standalone checkout and its selected historical outputs.

This record belongs to `main`. The source base commit at capture was `9e5a0e008b620ec3b5977fb0bbd33c0e4b3060bf`. `manifest.json` records the original source bytes, selected assets, and tracked deletions; these remain unchanged by the branch naming update.

Selected inputs and outputs for `crash_2023174894` and `crash_2023197392` are included. The historical output variants are available on `dev_old`; current outputs are on `main`. This repository is the personal `HaoweiLi1/TeraSim` fork; these changes are not pushed to `mcity/TeraSim`.

Excluded: .env and credentials; API response cache; Python caches and egg-info; rebuildable Cython .c/.so products. This preserves selected reproducibility assets, not every generated file.
